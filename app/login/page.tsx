'use client';
import Link from 'next/link';
import { signIn } from 'next-auth/react';
export default function LoginPage() { return <main className="mx-auto max-w-md px-4 py-14"><p className="eyebrow text-[#b85036]">WELCOME BACK</p><h1 className="mt-3 font-serif text-4xl font-semibold">Log in and join the conversation.</h1><div className="mt-7 rounded-2xl bg-white p-6 ring-1 ring-stone-200"><p className="text-sm leading-6 text-stone-600">Sign in to share your two cents, comment, and let people know when you feel the same.</p><button onClick={() => signIn('google', { callbackUrl: '/community' })} className="mt-5 w-full rounded-xl bg-ink py-3 font-bold text-white">Continue with Google</button><button onClick={() => signIn('apple', { callbackUrl: '/community' })} className="mt-3 w-full rounded-xl border border-stone-300 py-3 font-bold">Continue with Apple</button><p className="mt-5 text-center text-sm text-stone-500">New here? <Link href="/signup" className="font-bold text-[#b85036] underline">Create a free account</Link></p></div></main>; }
