import { SearchBox } from '@/components/ui';
import { businesses, reviews } from '@/lib/data';
import Link from 'next/link';
export default function Tea() { return <main className="mx-auto max-w-5xl px-4 py-12"><p className="eyebrow text-[#b85036]">THE REVIEW KNOWLEDGE BASE</p><h1 className="mt-3 font-serif text-5xl font-semibold">What&apos;s the Tea?</h1><p className="mt-4 max-w-2xl text-lg leading-7 text-stone-600">Search real reviews and practical advice before you spend your time or money.</p><div className="mt-7 max-w-2xl"><SearchBox /></div><section className="mt-12"><h2 className="font-serif text-3xl font-semibold">Fresh from the community</h2><div className="mt-5 space-y-4">{reviews.map(review => <Link href={`/review/${review.id}`} key={review.id} className="block rounded-2xl border border-stone-200 bg-white p-6"><p className="text-sm font-bold text-[#b85036]">{review.badge}</p><h3 className="mt-2 text-xl font-bold">{review.title}</h3><p className="mt-2 text-stone-600">{review.body}</p><p className="mt-4 text-sm font-bold">From {review.name} · {review.helpful} found this helpful</p></Link>)}</div></section><section className="mt-12"><h2 className="font-serif text-3xl font-semibold">Browse the deal by category</h2><div className="mt-5 flex flex-wrap gap-3">{[...new Set(businesses.map(b => b.category))].map(category => <Link key={category} href={`/search?q=${encodeURIComponent(category)}`} className="rounded-full border border-stone-300 bg-white px-4 py-2 text-sm font-bold">{category}</Link>)}</div></section></main>; }
