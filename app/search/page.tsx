import {businesses} from '@/lib/data'; import {BusinessCard,SearchBox} from '@/components/ui';
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=''}=await searchParams; const matches=businesses.filter(b=>`${b.name} ${b.category} ${b.location}`.toLowerCase().includes(q.toLowerCase())); return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Find your next favorite</h1><div className="mt-6 max-w-2xl"><SearchBox/></div><div className="mt-10 grid gap-8 lg:grid-cols-[220px_1fr]"><aside className="rounded-2xl bg-white p-5 ring-1 ring-stone-200"><b>Filter results</b><div className="mt-5 space-y-4 text-sm"><label className="block">Rating<select className="mt-1 w-full rounded border p-2"><option>Any rating</option><option>4 stars & up</option></select></label><label className="block">Distance<select className="mt-1 w-full rounded border p-2"><option>Any distance</option><option>Within 5 miles</option></select></label><label className="flex gap-2"><input type="checkbox"/> Open now</label></div></aside><section><p className="mb-5 text-stone-500">{matches.length} businesses {q && `for “${q}”`}</p><div className="grid gap-5 sm:grid-cols-2">{matches.map(b=><BusinessCard business={b} key={b.id}/>)}</div></section></div></main>}
