import type { Metadata } from 'next'; import './globals.css'; import { SiteHeader } from '@/components/site-header'; import { AuthProvider } from '@/components/auth-provider';
export const metadata: Metadata = { title: 'Unsolicited Advice | Give us your two cents', description: 'Honest thoughts, unfiltered. Join the conversation and share your two cents.', manifest: '/manifest.json', icons: { icon: '/icon.svg' }, openGraph: { title: 'Unsolicited Advice | My Two Cents', description: 'Honest thoughts, unfiltered. Join the conversation.', images: ['/opengraph-image'] }, twitter: { card: 'summary_large_image', title: 'Unsolicited Advice | My Two Cents', description: 'Honest thoughts, unfiltered. Join the conversation.' } };
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body><AuthProvider><SiteHeader />{children}</AuthProvider><script dangerouslySetInnerHTML={{ __html: "if('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js')" }} /></body></html>; }
