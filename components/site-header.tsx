'use client';
import Link from 'next/link';
import { signOut, useSession } from 'next-auth/react';
export function SiteHeader() { const { data: session, status } = useSession(); const name = session?.user?.name?.split(' ')[0]; return <header className="sticky top-0 z-20 border-b border-stone-200 bg-[#fffaf5]/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-4"><Link href="/" className="text-xl font-black tracking-tight">Unsolicited <span className="text-brand">Advice</span></Link><nav className="hidden gap-6 text-sm font-semibold md:flex"><Link href="/search">Explore</Link><Link href="/community">Community</Link><Link href="/business/claim">For businesses</Link></nav><div className="flex items-center gap-2">{status === 'authenticated' ? <><Link className="rounded-full px-3 py-2 text-sm font-semibold" href="/community">Hi, {name}</Link><button onClick={() => signOut({ callbackUrl: '/' })} className="rounded-full bg-ink px-4 py-2 text-sm font-semibold text-white">Log out</button></> : <><Link className="rounded-full px-4 py-2 text-sm font-semibold" href="/login">Log in</Link><Link className="rounded-full bg-ink px-4 py-2 text-sm font-semibold text-white" href="/signup">Join free</Link></>}</div></div></header>; }
