import Link from 'next/link';

export const Stars = ({ value }: { value: number }) => <span className="font-bold text-amber-500" aria-label={`${value} out of 5 stars`}>{'★'.repeat(Math.round(value))}<span className="text-stone-300">{'★'.repeat(5 - Math.round(value))}</span></span>;
export const FactsBadge = ({ count }: { count?: number }) => <button type="button" className="rounded-full bg-[#10221e] px-3 py-1.5 text-xs font-black tracking-[.12em] text-[#c8f169]">↑ FACTS{count !== undefined ? ` ${count}` : ''}</button>;
export function SearchBox() { return <form action="/my-two-cents" className="flex rounded-2xl bg-white p-2 shadow-lg ring-1 ring-stone-200"><input name="q" aria-label="Search the My Two Cents" className="min-w-0 flex-1 px-3 outline-none" placeholder="Business, category, city, or ZIP" /><button className="rounded-xl bg-brand px-5 py-3 font-bold text-white">Search My Two Cents</button></form>; }
export function BusinessCard({ business }: { business: any }) { return <Link href={`/business/${business.id}`} className="group rounded-2xl border border-stone-200 bg-white p-5 transition hover:-translate-y-1 hover:shadow-lg"><div className="mb-4 flex justify-between"><span className="text-3xl">{business.image}</span><span className={business.open ? 'rounded-full bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700' : 'rounded-full bg-stone-100 px-2 py-1 text-xs font-bold'}>{business.open ? 'Open now' : 'Closed'}</span></div><p className="text-xs font-bold uppercase tracking-wider text-brand">Registry file · {business.category}</p><h3 className="mt-1 text-lg font-bold">{business.name}</h3><p className="mt-1 text-sm text-stone-500">{business.location} · {business.price}</p><div className="mt-4 flex items-center gap-2"><Stars value={business.rating} /><b>{business.rating}</b><span className="text-sm text-stone-500">({business.reviews} reports)</span></div></Link>; }
